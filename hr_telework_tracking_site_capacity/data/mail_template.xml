<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">

    <!--
    SIMPLIFIED NOTIFICATION SYSTEM:
    - Managers get Odoo notifications (bus.bus) for:
      * Weekly automatic generation
      * Weekly automatic confirmation
      * Pending review entries
    - ALL users (employees and managers) receive weekly email with:
      * Table of their assignments
      * PDF report attached
    - No other email notifications are sent
    -->

    <!-- TEMPLATES REMOVED - NOT USED ANYMORE:
         - mail_telework_overbooked (now Odoo notification only)
         - mail_reminder_undeclared (feature removed)
         - mail_employee_weekly_assignments (replaced by direct mail.mail)
    -->

  </data>
</odoo>
