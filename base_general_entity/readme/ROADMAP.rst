* Add ``tracking=True`` to key fields (e.g. ``entity_global_code``,
  ``is_primary_entity``, ``is_secondary_entity``) so that changes are logged
  automatically in the chatter.
* Add ``search_default_filter_active`` to action contexts so that only active
  records are shown by default.
* Portal views: the portal group already has read access to relationships, but
  no portal controllers or templates exist yet.
