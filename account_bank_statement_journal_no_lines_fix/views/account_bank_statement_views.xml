<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_bank_statement_form_fix" model="ir.ui.view">
        <field name="name">account.bank.statement.form.fix</field>
        <field name="model">account.bank.statement</field>
        <field name="inherit_id" ref="account_statement_base.view_bank_statement_form"/>
        <field name="arch" type="xml">
            <field name="journal_id" position="attributes">
                <attribute name="invisible">0</attribute>
                <attribute name="readonly">0</attribute>
                <attribute name="required">1</attribute>
                <attribute name="domain">[('type', 'in', ['bank', 'cash'])]</attribute>
                <attribute name="options">{'no_create': True}</attribute>
            </field>

            <field name="date" position="attributes">
                <attribute name="readonly">0</attribute>
            </field>

            <field name="balance_start" position="attributes">
                <attribute name="help">The starting balance for this statement period</attribute>
            </field>

            <field name="balance_end_real" position="attributes">
                <attribute name="help">The real ending balance as shown on your bank statement. This should match your bank statement's final balance.</attribute>
            </field>
        </field>
    </record>

</odoo>
