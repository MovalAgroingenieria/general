1. Create a vote type with code, result type, formula and partner domain.
2. Use *Recompute votes* on a vote type to calculate values for matching
   partners.
3. Review generated partner votes from the vote type form or partner form.
4. Use *Recompute my votes* on a partner to refresh only that partner lines.
