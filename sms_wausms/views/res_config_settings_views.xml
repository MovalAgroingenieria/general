<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form_inherit_sms_wausms" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.sms.wausms</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="0"/>
        <field name="inherit_id" ref="sms.res_config_settings_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//widget[@service_name='sms']" position="before">
                <field name="sms_provider"
                       class="o_light_label"
                       widget="radio"
                       options="{'horizontal': true}"
                       required="True"/>

                <div class="content-group" invisible="sms_provider != 'wausms'">
                    <group>
                        <field name="sms_wausms_service_url"/>
                        <field name="sms_wausms_api_user"/>
                        <field name="sms_wausms_api_passwd" password="1"/>
                        <field name="sms_wausms_sender"/>
                        <div class="text-muted">
                            Limit to 15 numbers or 11 alphanumeric characters.
                        </div>
                    </group>
                </div>
            </xpath>

            <!-- Hide the default SMS widget when using WauSMS -->
            <xpath expr="//widget[@service_name='sms']" position="attributes">
                <attribute name="invisible">sms_provider == 'wausms'</attribute>
            </xpath>

        </field>
    </record>

</odoo>
