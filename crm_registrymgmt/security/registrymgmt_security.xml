<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Categoría del módulo -->
        <record id="res_registrymgmt_module_category" model="ir.module.category">
            <field name="name">Letter Management</field>
            <field name="sequence">10</field>
        </record>

        <!-- Grupo Portal -->
        <record id="res_registrymgmt_group_portal" model="res.groups">
            <field name="name">Portal</field>
            <field name="category_id" ref="res_registrymgmt_module_category"/>
            <field name="users" eval="[(4, ref('base.user_demo'))]"/>
        </record>

        <!-- Grupo Usuario -->
        <record id="res_registrymgmt_group_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="res_registrymgmt_module_category"/>
        </record>

        <!-- Grupo Administrador -->
        <record id="res_registrymgmt_group_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="res_registrymgmt_module_category"/>
            <field name="implied_ids" eval="[(4, ref('res_registrymgmt_group_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_admin'))]"/>
        </record>

    </data>
</odoo>
