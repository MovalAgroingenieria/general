<?xml version="1.0" encoding="UTF-8" ?>
<odoo noupdate="1">
    <!-- Default assembly type. Document texts are set here (not as Python field
         defaults) so they are translatable model terms exported to the .po files;
         address fields are left unset so they default from the company. -->
    <record id="assembly_type_general" model="assembly.type">
        <field name="name">General Assembly</field>
        <field
            name="vote_type_ids"
            eval="[(4, ref('base_vote.vote_type_general'))]"
        />
        <field name="default_publication_text" type="html">
            <h2>{{ object.name }}</h2>
            <p>{{ format_datetime(object.date_first_call) if object.date_first_call else '' }}{{ ' — ' + object.location if object.location else '' }}</p>
            <p>This notice is the official convocation for the assembly above. Members are requested to attend at the date, time and venue indicated, and to review the agenda.</p>
        </field>
        <field name="default_delegation_document_text" type="html">
            <p>Vote delegation form for the assembly <strong>{{ object.name }}</strong>.</p>
            <p>Identify the delegator and the delegate below and specify the vote types delegated.</p>
        </field>
        <field name="default_delegation_footer_text" type="html">
            <p>The delegator certifies the accuracy of the information above and authorizes the delegate to cast votes on their behalf for the indicated vote type(s).</p>
        </field>
        <field name="default_representation_document_text" type="html">
            <p>Representation form for the assembly <strong>{{ object.name }}</strong>.</p>
            <p>The represented member appoints the representative below to attend and act on their behalf at this assembly.</p>
        </field>
        <field name="default_ballot_intro_text" type="html">
            <p>Voting ballot for <strong>{{ object.name }}</strong>.</p>
            <p>Mark your choice clearly for each agenda item.</p>
        </field>
        <field name="default_ballot_nominative_intro_text" type="html">
            <p>Personalized voting ballot for the assembly <strong>{{ object.name }}</strong>.</p>
        </field>
        <field name="default_final_text" type="html">
            <p>What is set out above is hereby notified for your information and attendance.</p>
        </field>
    </record>
</odoo>
