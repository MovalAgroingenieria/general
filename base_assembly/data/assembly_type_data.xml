<?xml version="1.0" encoding="UTF-8" ?>
<odoo noupdate="1">
    <!-- Default assembly type: text and address fields are left unset so the
         model defaults apply (default texts from the type constants; address
         copied from the company only if it has one). -->
    <record id="assembly_type_general" model="assembly.type">
        <field name="name">General Assembly</field>
    </record>
</odoo>
