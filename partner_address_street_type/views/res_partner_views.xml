<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">Add separate fields for street type</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="street" position="attributes">
                <attribute name="style">width:88%; display:inline-block; margin-left:2%;</attribute>
            </field>
            <field name="street" position="before">
                <field
                        name="street_type_id"
                        modifiers="{'readonly': [('type', '=', 'contact'), ('parent_id', '!=', False)]}"
                        context="{'in_combo': True}"
                        domain="[('show_in_list', '=', True)]"
                        groups="base.group_no_one"
                        options="{'no_open': True, 'no_quick_create': True, 'no_create_edit': True}"
                        style="width:10%;"
                        placeholder="Street type"
                />
            </field>
        </field>
    </record>

    <record id="view_partner_address_form" model="ir.ui.view">
        <field name="name">Add separate fields for street type (edition form)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form"/>
        <field name="arch" type="xml">
            <field name="street" position="attributes">
                <attribute name="style">width:88%; display:inline-block; margin-left:2%;</attribute>
            </field>
            <field name="street" position="before">
                <field
                        name="street_type_id"
                        modifiers="{'readonly': [('type', '=', 'contact'), ('parent_id', '!=', False)]}"
                        context="{'in_combo': True}"
                        domain="[('show_in_list', '=', True)]"
                        groups="base.group_no_one"
                        options="{'no_open': True, 'no_quick_create': True, 'no_create_edit': True}"
                        style="width:10%;"
                        placeholder="Street type"
                />
            </field>
        </field>
    </record>

    <!-- res.partner main form:
 Only touch the inline form under child_ids -->
    <record id="view_partner_form_inherit_child_ids_street_type" model="ir.ui.view">
        <field name="name">res.partner.form: child_ids street_type_id injection</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="priority">30</field>
        <field name="arch" type="xml">

            <!-- 1) Narrow the street field ONLY inside the child_ids embedded form -->
            <xpath expr="//field[@name='child_ids']/form//field[@name='street']" position="attributes">
                <attribute name="style">width:88%; display:inline-block; margin-left:2%;</attribute>
            </xpath>

            <!-- 2) Insert street_type_id before street in that same embedded form -->
            <xpath expr="//field[@name='child_ids']/form//field[@name='street']" position="before">
                <field
                        name="street_type_id"
                        modifiers="{'readonly': [('type', '=', 'contact')]}"
                        context="{'in_combo': True}"
                        domain="[('show_in_list', '=', True)]"
                        groups="base.group_no_one"
                        options="{'no_open': True, 'no_quick_create': True, 'no_create_edit': True}"
                        style="width:10%;"
                        placeholder="Street type"
                />
            </xpath>
            <!-- 3) Add street_type_id to child_ids context -->
            <field name="child_ids" position="attributes">
                <attribute name="context">{
                    'default_parent_id': id,
                    'default_street': street,
                    'default_street2': street2,
                    'default_city': city,
                    'default_state_id': state_id,
                    'default_zip': zip,
                    'default_country_id': country_id,
                    'default_lang': lang,
                    'default_user_id': user_id,
                    'default_type': 'other',
                    'default_street_type_id': street_type_id
                    }
                </attribute>
            </field>
        </field>
    </record>

</odoo>
